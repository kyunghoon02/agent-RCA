---
- name: Deploy and verify the Neo4j StateGraph
  hosts: control_plane
  become: true
  gather_facts: true
  vars_files:
    - ../group_vars/all.yml
    - ../../../platform/versions.yaml

  roles:
    - role: stategraph_stack
    - role: stategraph_verify
