---
- name: Verify the frozen RCA runtime before repeated evaluation
  hosts: rca_control
  become: true
  gather_facts: false
  vars_files:
    - ../group_vars/all.yml
    - ../../../platform/versions.yaml

  roles:
    - role: evaluation_runtime_verify
