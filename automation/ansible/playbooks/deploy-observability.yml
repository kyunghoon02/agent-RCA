---
- name: Deploy and verify the single-node observability stack
  hosts: control_plane
  become: true
  gather_facts: true
  vars_files:
    - ../group_vars/all.yml

  roles:
    - role: observability_stack
    - role: cilium
    - role: cluster_verify
    - role: observability_verify
