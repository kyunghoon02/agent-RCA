---
- name: Verify the private Incident Viewer frontend
  hosts: rca_control
  become: true
  gather_facts: false
  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/default-profile.yml
    - ../../../platform/versions.yaml

  roles:
    - role: incident_viewer_frontend_verify
